{{ define "body" }}
<p>This server has not been configured yet, so it is not storing or serving anything.</p>
<p>Open <a href="{{ .BaseURL }}/install">{{ .BaseURL }}/install</a> to finish the install.</p>
{{ end }}
