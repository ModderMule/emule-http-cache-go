{{ define "body" }}
<p>This server has a config file that the installer did not write, so there is
nothing for this page to do &mdash; and it will not read your key back to you.</p>
<p>The key is in the config file, where you put it.</p>
<p class="muted"><a href="{{ .BaseURL }}/">Server status</a></p>
{{ end }}
